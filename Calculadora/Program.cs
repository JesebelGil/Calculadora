using System;

namespace Calculadora
{
    class Program
    {
        static void Main(string[] args)
        {
            var calculadora = new Calculadora("Ingrese la operacion que se desea realizar (+, -, *, /), si quiere salir, escriba (salir) ");
            calculadora.Start();   
            
        }

}       //out method, ref method, new, return, void return, declare arguments, constructors.

}
